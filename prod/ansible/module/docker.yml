---

- name: init docker env
  hosts: all
  serial: 1
  sudo: yes
  gather_facts: yes
  # tasks:
  #   - command: /usr/bin/docker
  #     register: result
  #     ignore_errors: True
  roles:
    - docker
      # when: result | failed
    - docker-configuration
